# Server
PORT=3001
MAX_JSON=2mb

# Email sender identity
MAIL_FROM="Bucks2Bar <no-reply@example.com>"
# Optional default recipient when request has no email
MAIL_TO=

# Option 1: Full SMTP URL
# SMTP_URL="smtp://user:pass@smtp.mailhost.com:587"

# Option 2: Host and credentials
# SMTP_HOST=smtp.mailhost.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=

# If neither SMTP_URL nor SMTP_HOST is provided, server will use Nodemailer Ethereal for dev and return a preview URL.
